<UserControl x:Class="WindowsUtilityPack.Tools.DeveloperProductivity.Base64Encoder.Base64EncoderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="4" MaxWidth="900">
            <TextBlock Text="Base64 / URL / HTML Encoder" Style="{StaticResource ToolTitleTextStyle}" Margin="0,0,0,16"/>

            <!-- Controls -->
            <Border Style="{StaticResource ToolPanelBorderStyle}" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Mode:" Width="80" VerticalAlignment="Center"
                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <ComboBox ItemsSource="{Binding Modes}" SelectedItem="{Binding Mode}"
                                  Width="160" Padding="8,4"/>
                        <TextBlock Text="Encoding:" Width="80" VerticalAlignment="Center" Margin="20,0,0,0"
                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <ComboBox ItemsSource="{Binding Encodings}" SelectedItem="{Binding Encoding}"
                                  Width="100" Padding="8,4"
                                  IsEnabled="{Binding IsBase64Mode}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Encode" Command="{Binding EncodeCommand}"
                                Style="{StaticResource PrimaryActionButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Decode" Command="{Binding DecodeCommand}"
                                Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="⇅ Swap" Command="{Binding SwapCommand}"
                                Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Copy Output" Command="{Binding CopyOutputCommand}"
                                Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Clear" Command="{Binding ClearCommand}"
                                Style="{StaticResource SecondaryActionButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Input -->
            <Border Style="{StaticResource ToolPanelBorderStyle}" Padding="0" Margin="0,0,0,4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>
                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                            BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="0,0,0,1"
                            Padding="12,8">
                        <TextBlock Text="Input" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </Border>
                    <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12"
                             Foreground="{DynamicResource PrimaryTextBrush}"
                             BorderThickness="0" Padding="12"/>
                </Grid>
            </Border>

            <!-- Direction indicator -->
            <TextBlock Text="▼" TextAlignment="Center" FontSize="18"
                       Foreground="{DynamicResource AccentBrush}" Margin="0,4"/>

            <!-- Output -->
            <Border Style="{StaticResource ToolPanelBorderStyle}" Padding="0" Margin="0,4,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>
                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                            BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="0,0,0,1"
                            Padding="12,8">
                        <TextBlock Text="Output" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </Border>
                    <TextBox Grid.Row="1" Text="{Binding OutputText, Mode=OneWay}"
                             IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12"
                             Foreground="{DynamicResource PrimaryTextBrush}"
                             BorderThickness="0" Padding="12"/>
                </Grid>
            </Border>

            <!-- Status -->
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontSize="11" Margin="4,0,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
