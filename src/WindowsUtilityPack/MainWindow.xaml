<Window x:Class="WindowsUtilityPack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:WindowsUtilityPack.Controls"
        xmlns:vm="clr-namespace:WindowsUtilityPack.ViewModels"
        mc:Ignorable="d"
        Title="Windows Utility Pack"
        Width="1100" Height="700"
        MinWidth="900" MinHeight="550"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Closing="OnWindowClosing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo + Title (clickable → home) -->
                <Button Grid.Column="0" Style="{StaticResource TransparentButtonStyle}"
                        Command="{Binding NavigateHomeCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="⚙" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"
                                   Foreground="{DynamicResource AccentBrush}"/>
                        <TextBlock Text="Windows Utility Pack"
                                   FontSize="20" FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </StackPanel>
                </Button>

                <!-- Theme Toggle -->
                <Button Grid.Column="2"
                        Style="{StaticResource ThemeToggleButtonStyle}"
                        Command="{Binding ToggleThemeCommand}"
                        Content="{Binding ThemeToggleIcon}"
                        VerticalAlignment="Center"
                        ToolTip="Toggle Dark/Light Mode"/>
            </Grid>
        </Border>

        <!-- Category Navigation Bar - data-driven from MainWindowViewModel.Categories -->
        <Border Grid.Row="1"
                Background="{DynamicResource NavBarBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,10">
            <ItemsControl ItemsSource="{Binding Categories}" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:CategoryNavItem}">
                        <controls:CategoryMenuButton
                            Label="{Binding Label}"
                            Icon="{Binding Icon}"
                            MenuItems="{Binding MenuItems}"
                            NavigateCommand="{Binding DataContext.NavigateCommand,
                                             RelativeSource={RelativeSource AncestorType=Window}}"
                            Margin="4,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Main Content Area — DataTemplates resolve the correct View automatically -->
        <ContentControl Grid.Row="2"
                        Content="{Binding CurrentView}"
                        Margin="20"/>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontSize="12"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
