<Window x:Class="WindowsUtilityPack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:WindowsUtilityPack.Controls"
        mc:Ignorable="d"
        Title="Windows Utility Pack"
        Width="1100" Height="700"
        MinWidth="900" MinHeight="550"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        Background="{DynamicResource AppBackgroundBrush}"
        Closing="OnWindowClosing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- ── Header ── -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo + Title → home -->
                <Button Grid.Column="0" Style="{StaticResource TransparentButtonStyle}"
                        Command="{Binding NavigateHomeCommand}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets"
                                   FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"
                                   Foreground="{DynamicResource AccentBrush}"/>
                        <TextBlock Text="Windows Utility Pack"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </StackPanel>
                </Button>

                <!-- Theme Toggle -->
                <Button Grid.Column="2"
                        Style="{StaticResource ThemeToggleButtonStyle}"
                        Command="{Binding ToggleThemeCommand}"
                        FontFamily="Segoe MDL2 Assets"
                        Content="{Binding ThemeToggleIcon}"
                        VerticalAlignment="Center"
                        ToolTip="Toggle Dark / Light Mode"
                        Margin="0,0,6,0"/>

                <!-- Settings Gear -->
                <Button Grid.Column="3"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding OpenSettingsCommand}"
                        Content="&#xE713;"
                        VerticalAlignment="Center"
                        ToolTip="Settings"/>
            </Grid>
        </Border>

        <!-- ── Category Navigation Bar ── -->
        <Border Grid.Row="1"
                Background="{DynamicResource NavBarBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <UniformGrid Rows="1" Columns="5" HorizontalAlignment="Stretch">
                <controls:CategoryMenuButton
                    Label="System Utilities"
                    Icon="&#xE770;"
                    NavigateCommand="{Binding NavigateCommand}"
                    Margin="4,0">
                    <controls:CategoryMenuButton.MenuItems>
                        <controls:MenuEntry Label="Storage Master" ToolKey="storage-master"/>
                        <controls:MenuEntry Label="Task Manager Plus" ToolKey=""/>
                        <controls:MenuEntry Label="Startup App Control" ToolKey=""/>
                    </controls:CategoryMenuButton.MenuItems>
                </controls:CategoryMenuButton>

                <controls:CategoryMenuButton
                    Label="File &amp; Data Tools"
                    Icon="&#xE8B7;"
                    NavigateCommand="{Binding NavigateCommand}"
                    Margin="4,0">
                    <controls:CategoryMenuButton.MenuItems>
                        <controls:MenuEntry Label="Bulk File Renamer" ToolKey="bulk-renamer"/>
                        <controls:MenuEntry Label="Duplicate Finder" ToolKey=""/>
                        <controls:MenuEntry Label="CSV / JSON Tools" ToolKey=""/>
                    </controls:CategoryMenuButton.MenuItems>
                </controls:CategoryMenuButton>

                <controls:CategoryMenuButton
                    Label="Security &amp; Privacy"
                    Icon="&#xE72E;"
                    NavigateCommand="{Binding NavigateCommand}"
                    Margin="4,0">
                    <controls:CategoryMenuButton.MenuItems>
                        <controls:MenuEntry Label="Password Generator" ToolKey="password-generator"/>
                        <controls:MenuEntry Label="Privacy Dashboard" ToolKey=""/>
                        <controls:MenuEntry Label="Secure File Shredder" ToolKey=""/>
                    </controls:CategoryMenuButton.MenuItems>
                </controls:CategoryMenuButton>

                <controls:CategoryMenuButton
                    Label="Network &amp; Internet"
                    Icon="&#xE774;"
                    NavigateCommand="{Binding NavigateCommand}"
                    Margin="4,0">
                    <controls:CategoryMenuButton.MenuItems>
                        <controls:MenuEntry Label="Ping Tool" ToolKey="ping-tool"/>
                        <controls:MenuEntry Label="Network Scanner" ToolKey=""/>
                        <controls:MenuEntry Label="DNS Utility" ToolKey=""/>
                    </controls:CategoryMenuButton.MenuItems>
                </controls:CategoryMenuButton>

                <controls:CategoryMenuButton
                    Label="Developer &amp; Productivity"
                    Icon="&#xE943;"
                    NavigateCommand="{Binding NavigateCommand}"
                    Margin="4,0">
                    <controls:CategoryMenuButton.MenuItems>
                        <controls:MenuEntry Label="Text Format Converter" ToolKey="text-format-converter"/>
                        <controls:MenuEntry Label="Regex Tester" ToolKey="regex-tester"/>
                        <controls:MenuEntry Label="Text Diff Tool" ToolKey=""/>
                        <controls:MenuEntry Label="Clipboard Manager" ToolKey=""/>
                    </controls:CategoryMenuButton.MenuItems>
                </controls:CategoryMenuButton>
            </UniformGrid>
        </Border>

        <!-- ── Main Content Area ── -->
        <ContentControl Grid.Row="2"
                        Content="{Binding CurrentView}"
                        Margin="24,16"/>

        <!-- ── Status Bar ── -->
        <Border Grid.Row="3"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontSize="11"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
